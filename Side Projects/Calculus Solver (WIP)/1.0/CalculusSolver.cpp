/**
 * @author Clayton Moeck
 * @brief Calculus Solver
 */

#include <iostream>
#include <string>

using namespace std;



int main() { 
  string equation;
  cout << "Enter the Equation: " << endl;
  cin >> equation;
  cout << equation;
  
  // while ((constant != "q" && constant != "Q") && (exponent != "q" && exponent != "Q")) {
  //   int constNum = stoi(constant);
  //   int expoNum = stoi(exponent);
  //   int deriv_constant = constNum * expoNum;
  //   int deriv_exponent = expoNum - 1;
  //   string dConst = std::to_string(deriv_constant);
  //   string dExpo = std::to_string(deriv_exponent);
  //   cout << dConst + "x" + "^" + dExpo << endl;

  //   cout << "Enter the Constant: " << endl;
  //   cin >> constant;
  //   cout << "Enter the Exponent: " << endl;
  //   cin >> exponent;
  // }

}